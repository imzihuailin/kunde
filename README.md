# Kunde（坤德）

一个全屏电子书阅读器，复刻了微信读书的背景。

可以改字体、字号、背景。

本地存储。

## 本地部署指南

安装依赖：

```bash
npm install
```

启动开发环境：

```bash
npm run dev
```

构建生产版本：

```bash
npm run build
```

本地预览构建结果：

```bash
npm run preview
```
